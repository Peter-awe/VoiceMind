{
  "name": "kiwipennotes-desktop",
  "version": "1.0.0",
  "description": "KiwiPenNotes — AI meeting transcription & translation, desktop app for macOS",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-mac": "electron-builder --mac --publish never",
    "build-dmg": "electron-builder --mac dmg --publish never",
    "release": "electron-builder --mac --publish always"
  },
  "author": "KiwiPenNotes",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8"
  }
}
